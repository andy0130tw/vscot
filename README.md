# vscot - VS Code Offset Tracker

Compat layer to track VS Code's document offsets across edits with CodeMirror's state library

CodeMirror's state library is added as a submodule for optimal treeshaking.
